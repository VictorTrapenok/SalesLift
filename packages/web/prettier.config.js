/**
 * Конфигурация Prettier для фронтенда.
 *
 * Конфига раньше не было, и `prettier --check` в CI сверял код с дефолтами —
 * то есть требовал двойные кавычки и перенос по 80 символов, чего в коде
 * никогда не было. Здесь зафиксирован фактический стиль проекта.
 *
 * @type {import('prettier').Config}
 */
export default {
  // Одинарные кавычки — так написан весь фронтенд.
  singleQuote: true,
  // 120 символов, как `line-length` у ruff на бэкенде (packages/api/pyproject.toml):
  // разная ширина строки в двух половинах монорепозитория — лишний повод для спора.
  printWidth: 120,
};
