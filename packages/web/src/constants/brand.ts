/**
 * Конфигурация брендинга.
 *
 * Все текстовые и визуальные атрибуты бренда собраны здесь, чтобы ребрендинг
 * или white-label не требовал правок по всему коду. В компонентах хардкодить
 * название продукта нельзя — только через BRAND.
 */
export const BRAND = {
  /** Краткое имя продукта (заголовки, шапка) */
  name: 'SalesLift',
  /** Полное имя (подписи, копирайт) */
  fullName: 'SalesLift',
  /** Ключ токена авторизации в localStorage */
  tokenKey: 'saleslift_token',
  /** Страница по умолчанию после входа */
  homePage: '/employees',
} as const;
